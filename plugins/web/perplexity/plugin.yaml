name: web-perplexity
version: 1.0.0
description: "Perplexity Sonar — search-grounded LLM answers with citations. Unique among web backends: returns a synthesized answer alongside the source rows. Requires PERPLEXITY_API_KEY — get one at https://www.perplexity.ai/settings/api."
author: NousResearch
kind: backend
provides_web_providers:
  - perplexity
